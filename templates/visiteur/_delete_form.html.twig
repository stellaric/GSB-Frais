<form method="post" action="{{ path('app_visiteur_delete', {'id': visiteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visiteur.id) }}">
    <button class="btn">Delete</button>
</form>
